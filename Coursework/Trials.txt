try 1 
	private Individual select() {	
		
		Individual parent=null;
		
		double fitnessSum=0;
		
		for (int i = 0; i<population.size();i++)
		{
			fitnessSum+=  population.get(i).fitness;
			
		}
		
		//Return Random number between 0 and max fitness
		Random r = new Random();
		double randomValue = fitnessSum * r.nextDouble();	
		
		fitnessSum = 0;
		Individual selected  = null;
		for (int i = 0; i < population.size();i++){
			selected = population.get(i);
			fitnessSum += selected.fitness;
			if(fitnessSum > randomValue){
				break;
			}
		}
		
		parent=selected;
		//Individual parent = population.get(Parameters.random.nextInt(Parameters.popSize));
		return parent.copy();
	}


The average fitness was 0.11137253682101775

try 2



The average fitness was 0.1548895248333837

trial 3
Fitness 0.16292873140221267


+ hidden layer weights ()only if you have 3, if it was hiddennodes*inputs)
+ number of hidden nodes
+ number of connection between hidden layer and output layer(number is hiddenlayers*outputlayer)
+ biases on output layer(number is the number of output)
